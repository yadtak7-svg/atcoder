#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);

    string S; cin >> S;
    int l; cin >> l;
    if(S.size() >= l) cout << "Yes" << endl;
    else cout << "No" << endl;
}